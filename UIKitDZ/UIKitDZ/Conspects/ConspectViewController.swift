//
//  ViewController.swift
//  UIKitDZ
//
//  Created by 2lup on 21.09.2022.
//

import UIKit

// Видеоурок 12

/*
/// ViewController
final class ConspectViewController: UIViewController {

    let myLabel = UILabel()
    
    override func viewDidLoad() {
        super.viewDidLoad()
        
        view.addSubview(myLabel)
        myLabel.frame = CGRect(x: 100, y: 100, width: 150, height: 50)
        myLabel.text = "IOS 11 xcode 123 IOS 11 xcode 123"
        myLabel.numberOfLines = 0
        myLabel.lineBreakMode = .byWordWrapping
        myLabel.font = UIFont.boldSystemFont(ofSize: 24)
        myLabel.sizeToFit()
        myLabel.adjustsFontSizeToFitWidth = true
        myLabel.textColor = .orange
        myLabel.shadowColor = .black
        myLabel.shadowOffset = CGSize(width: 2, height: 4)
        myLabel.center = view.center
    }

}
*/
