//
//  ConspectSceneDelegate.swift
//  UIKitDZ
//
//  Created by 2lup on 21.09.2022.
//

/*
import UIKit

/// ConspectSceneDelegate
class ConspectSceneDelegate: UIResponder, UIWindowSceneDelegate {

    // Фон для пользовательского интерфейса вашего приложения и объект, который отправляет события в ваши представления.
    var window: UIWindow?
    
    // стартовый метод для запуска приложения
    func scene(
        _ scene: UIScene,
        willConnectTo session: UISceneSession,
        options connectionOptions: UIScene.ConnectionOptions
    ) {
        // создаем экземпляр ConspectViewController
        let viewController = ConspectViewController()
        // передаем  viewControllerVC как корневой в NavigationController
        let navController = UINavigationController(rootViewController: viewController)
        // корневой VC для window, делаем им NavigationController
        window?.rootViewController = navController
        // задаем фон
        window?.backgroundColor = UIColor.white
        // Показывает окно и делает его ключевым окном.
        // window?.makeKeyAndVisible()
    }
}
 */
